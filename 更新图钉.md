# 更新图钉（愿望池整理）

> 基线：0.5.8-test（与正式库 0.5.8 同代码）。所有开发只在本测试库进行。
> 状态标记：⬜ 待做 · 🔨 进行中 · ✅ 完成 · ❓ 待复现/待讨论

## 优先级总览

1. UI 美化重写（作者主导，合并 B 组样式类需求）
2. A 悬浮球工具组（解决 TT/iOS 点不到退出键）
3. B/C 小需求打包（首行预览、预览字号、倒序、隐藏楼层可视化）
4. F 搜索跳转 + D 范围/批量导出
5. E 插件内消息编辑（工程量最大，最后做）
6. #4 折叠 bug（等复现信息）

🔨 **UI 重写进度（2026-09-05）**：主面板、卡片翻面、主题与阅读界面已接入测试版；13 项本地模拟 DOM 检查通过。浏览器视觉验收和运行中的 SillyTavern 联调待完成，详见 `UI设计参考.md`。

---

## A. 按键/布局可达性 —— #2 #8 #10

⬜ **可拖动悬浮球**：贴边收纳、记忆位置，展开后含阅读模式全部操作（退出/跳转/切页/设置）。作为所有壳浏览器（TT、via、iOS WebView）的兜底入口。
⬜ 设置项「按键布局」：默认 / 悬浮球 / 两者。不做更多套方案，控制维护成本。
- 根因：WebView 视口与 safe-area 计算差异导致固定定位按钮不可点。

## B. 阅读模式呈现 —— #1 #9

🔨 **对话/心里话文本样式**：引号对话、\*心里话\*、成对自定义标记、样式开关与独立配色已实现，本地检查通过，待浏览器和酒馆验证。**并入 UI 美化重写一起做。**
⬜ **楼层首行预览**：整理/档案列表每楼显示「#楼层号 + 前 N 字摘要」，便于定位剧情点。
⬜ **预览字体大小**：设置滑杆，CSS 变量实现。
⬜ **列表倒序**：最新楼层在前（默认倒序），偏好持久化。

## C. 楼层管理 —— #9 #7

⬜ **隐藏/显示楼层可视化**：每楼眼睛图标显示并切换隐藏状态，走 chat save API + 写回校验。
⬜ **档案筛选**：按消息条数筛选聊天文件（如 <1 或 >10 隐藏）。注意 jsonl 行数需懒加载/缓存，避免全量拉取。

## D. 导出/删除批量化 —— #3 #7

⬜ **范围导出**：导出对话框加起止楼层（第 X 楼到第 Y 楼）。
⬜ **批量导出（多选）**：角色聊天列表多选模式 + 批量导出。
⬜ **批量删除（多选）**：同上，需二次确认。

## E. 插件内消息编辑 —— #5

⬜ 阅读/整理模式下单层消息纯文本编辑（textarea），保存走既有写回+读取校验管线。
- 范围收窄：不做富文本、不做自动清洗错误标签（手动改是更稳的方案）。
- 场景：模型在正文中混入多余标签，摘取规则误配时手动修正。

## F. 搜索跳转 —— #6

⬜ 关键词搜索全部楼层（本地遍历），结果带上下文摘要，点击复用 floor jump 跳转。

## 待复现 Bug

❓ **#4 PC 端角色栏折叠不生效**：需要用户补充——哪个折叠控件、浏览器、控制台报错截图。

---

## 原始愿望池（存档）

1. 正文对话、心里话加样式
2. TT/iOS 阅读模式找不到退出键
3. 导出范围可选（几楼到几楼）
4. PC 角色栏折叠不生效
5. 插件内直接编辑文本（修多余标签）
6. 搜索关键词跳转楼层
7. 批量导出/批量删除（多选）、按消息条数筛选
8. TT 阅读模式点不到
9. 首行预览、显示/隐藏楼层可视化、预览字体大小、列表倒序
10. 作者：按键布局方案/悬浮窗工具组；重写 UI 美化优先
