# 本地 UI 检查

在仓库根目录运行：

```sh
node --test tests/ui-rewrite.test.cjs
```

需要支持 `node:test` 的 Node.js，无第三方依赖。测试读取实际 `index.js`，在隔离的模拟 DOM、存储与动画环境里验证控制逻辑及生成标签；禁止网络请求，不执行插件启动，不读取或修改真实聊天。

2026-09-05：15 项通过。涵盖长标题与封面回退、封面按钮往返翻面与焦点、窄卡阅读 / 跳转 / 编辑目标、正文预览缓存与纯文本处理、翻面锁、关闭 / 切页 / 切标签时清理、动效降级、主题切换，以及阅读对话 / 心里话样式与颜色刷新。

这些结果不代表浏览器或 SillyTavern 实测通过。模拟 DOM 不实现布局、绘制、图片加载、浏览器焦点规则或 Markdown 安全净化器；仍需在浏览器和运行中的酒馆检查实际效果与集成。
